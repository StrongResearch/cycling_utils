[tool.ruff]
line-length = 120
extend-exclude = [".venv", "docs", "docs/"]
[tool.black]
line-length = 120
extend-exclude = "docs/"
[tool.isort]
profile = "black"
[tool.mypy]
exclude = ["tests/",  "setup.py"]